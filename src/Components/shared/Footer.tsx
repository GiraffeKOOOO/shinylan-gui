import { Stack } from '@mui/material';
import { FC } from 'react';

const Footer: FC = () => {
  return (
    <Stack sx={{ height: '100px' }}>
      <p>footer</p>
    </Stack>
  );
};

export default Footer;
