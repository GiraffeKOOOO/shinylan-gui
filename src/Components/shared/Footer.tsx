import { FC } from 'react';
import { Stack } from '@mui/material';

const Footer: FC = () => {
  return (
    <Stack sx={{ height: '100px' }}>
      <p>footer</p>
    </Stack>
  );
};

export default Footer;
