import { Stack } from '@mui/material';
import { FC } from 'react';

const Navbar: FC = () => {
  return (
    <Stack sx={{ height: '100px' }}>
      <p>navbar</p>
    </Stack>
  );
};

export default Navbar;
